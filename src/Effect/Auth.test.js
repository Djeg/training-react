import * as User from './User'
import * as Eff from 'redux-saga/effects'
import * as UserState from '../State/User'

test(`
  it contains a root saga that takeEvery user actions
  and launch corresponding saga
`, () => {
  
});

test(`
  it can fetch the roles into the 
`, () => {
  
});
